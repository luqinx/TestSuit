apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'junit:junit:4.12'
}

sourceCompatibility = "7"
targetCompatibility = "7"


abkit {
    maven {
        groupId="chao.android.tools"
        artifactId="testsuit"
        versionName="0.0.1.dev1"
    }
}